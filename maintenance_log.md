# タイムカードアプリ メンテナンスログ

## 2026-04-21: 安定運用版への修正と復旧

### 解決された不具合
1. **スタッフ選択時の画面真っ白（クラッシュ）問題**
   - **原因**: スプレッドシートの過去のシートを削除した際、「スマホ側に記録があるが、スプレッドシート側にデータがない」という状態になった。同期ロジックにおいてこのケースの処理が抜けており、プログラムが `null` データを処理しようとしてクラッシュしていた。
   - **修正**: 「ローカルのみにデータがある場合」の分岐を追加し、安全にデータをマージするように修正。また、不正なデータが保存されていても自動で除去するガードを追加。

2. **接続テストボタンの「Invalid action」表示**
   - **原因**: ボタンがGASのURLにアクセスする際、必要な命令（?action=ping）を付与していなかった。
   - **修正**: `?action=ping` を付けてアクセスし、GASから `ok` の返答があれば成功と表示するように変更。

3. **GASコードの破損と文字化け**
   - **原因**: 前回の作業中にコードが途切れて保存され、日本語コメントが文字化けしていた。
   - **修正**: バックエンドコードを完全に再構築し、最新の日本語環境で復元。

### 現在の技術仕様（安定版: v2.5）
- **同期プロトコル**: 15秒以内の連続更新を防止するロック機構を搭載。
- **データ優先順位**: `clientUpdatedAt`（端末の時間）を最優先。古いバージョンのクラウドデータによる上書きを完全に防止。
- **論理削除（Tombstone）**: データの「完全削除」を行う際、12列目に `true` を書き込む仕様。これにより、他端末で削除したデータが復活するのを防止。

### 万が一の復旧手順
もし動作がおかしくなった場合は、以下のコミット時点のコードに戻してください：
- **Git Commit**: `0596e67` (fix: connection test button uses action=ping)
+
+## 2026-05-02: 利便性向上のための通知機能追加
+
+### 追加された機能
+1. **出勤時の退勤忘れ警告**
+   - **内容**: 出勤ボタンを押した際、過去の最新レコードに退勤打刻がない場合、警告ポップアップを表示。「履歴から修正」または「管理者へ報告」を促す。
+   - **目的**: 月末の集計作業時に発覚する「退勤漏れ」を、翌出勤時に即座に気づけるように改善。
+
+2. **6時間未満勤務の休憩確認**
+   - **内容**: 6時間未満で退勤ボタンを押した際、「休憩は取りましたか？」と確認ポップアップを表示。備考欄への記入を促す。
+   - **目的**: 短時間勤務で休憩が発生した際の記録漏れを防止。
+
+### 安全性への配慮
+- 既存のデータ保存ロジック（GAS連携等）には一切変更を加えておらず、画面表示上の処理追加のみに留めているため、運用への影響はない。
+

## 2026-07-09〜12: 交通費の専用欄・月次確認・Excel関数化（担当: Claude Code）

### 背景
スタッフの交通費記載漏れ・誤記載が2ヶ月連続で発生し、税理士から「何度も修正依頼されると別料金」と指摘があったため、「本人が入力した交通費を正とし、本人が責任を持って月末確認する」運用に変更。自動化はあえて最小限に留めた。

### 追加された機能
1. **打刻画面のレイアウト変更**: 出勤退勤→賄い（大型トグルボタン）→履歴→中抜け→備考の順に。賄い押し忘れ対策。
2. **交通費の専用数値欄**: 賄いボタンの横に配置。備考への「交通費あり往復○円」記載は廃止。
3. **「いつもの額」ボタン**: 本人が一度金額を登録すると次回からワンタップ入力（自動では入らない・本人タップ必須）。
4. **履歴画面に交通費列＋月間合計**を表示。
5. **「今月分を確認しました」ボタン**: 押した日時を記録しExcelにも出力（押し直すと日時が更新される）。
6. **Excel出力の関数化**: 交通費合計はSUM式で出力し、Excel上で数字を直せば合計も自動で再計算されるように。外注用の給与計算ブロック（勤務時間合計・報酬・請求合計）もすべてセル参照の数式に変更。
7. **修正履歴の自動記録**: スタッフがデータを修正すると、スプレッドシートの「EditLog」シートに差分が自動で残る。
8. **【本人修正済】タグはExcel出力時に自動除去**（連絡用の備考はそのまま残る）。

## 2026-07-14: 税理士様式テンプレートへの差し替えと有給欄の自動反映（担当: Claude Code）

### 背景
税理士から交通費欄を別列にした新様式（タイムカード入力_202606給与分 _交通費表示修正.xlsx）が共有された。アプリの出力（交通費=O列）と新様式（交通費=P列・備考=Q列）で列がズレていたため、様式に合わせて全面対応。

### 変更内容
1. **Excelテンプレートを税理士様式に差し替え**（EXCEL_TEMPLATE_B64を再生成）
   - シート名がスタッフのフルネーム（草野久美子など）になり、アプリの登録名と一致するように。
   - 交通費: O列→P列（P34にSUM式が様式内蔵）。備考: P列→Q列。P列の幅を12.5に拡大し「交通費」と金額が見えるように。
   - ※差し替え時のミスで計算式（勤務時間・計算時間等）まで消してしまい一度修正。教訓: テンプレートのクリアは入力列（C〜G, K, P, Q）のみに限定し、数式列（H, J, L, M, N）には触れないこと。
2. **有給欄の自動反映**（有休消化・残有休日数・次回発生）
   - 消化日数: アプリの有給申請データから当月分を自動集計。
   - 残日数・次回付与日: オーナー管轄の有給管理表（Googleドライブ「有給」フォルダの「年次有給休暇管理簿YYYY」）から読み取り専用で取得。管理表の更新自体は従来どおりオーナーの手作業（アプリからは一切書き込まない）。
   - 管理表は年ごとに新ファイルになるため、IDを固定せず対象月の年でファイル名検索。見つからない場合は出力時にアラート。
3. **GASに getLeaveInfo アクションを追加**（?action=getLeaveInfo&year=YYYY）
   - appsscript.json に oauthScopes（spreadsheets / drive.readonly / script.scriptapp）を明示。
   - 注意: ドライブ読み取りの初回承認が必要だった。承認ポップアップが出ない場合はマニフェストへのスコープ追記が必要（今回それが原因で1度ハマった）。
4. **異体字の名前照合対応**: 管理簿の「髙橋」（はしご高）とアプリの「高橋」が別文字扱いで「未登録」になったため、髙/高・﨑/崎・齋斉/斎・邊邉/辺・櫻/桜 を正規化して照合するようGAS・アプリ両方に実装。
5. **出力速度の改善**: クラウド同期と有給管理表取得を並行実行に変更（直列だと1分近くかかっていた）。

### GASデプロイ履歴
- バージョン24 (2026-07-14 15:07): getLeaveInfo追加・P列対応
- バージョン25 (2026-07-14): 異体字照合対応

### 万が一の復旧手順
テンプレート差し替え前の状態に戻す場合は Git Commit `cd697f4` 時点の timecard.html を使用。

## 2026-07-14: スタッフの表示順変更機能（担当: Claude Code）

### 背景
スタッフの入籍による改姓に対応するため。名前の直接書き換えは、打刻データ・シート名が「スタッフ名」を鍵に
しているため危険（複数端末に旧名データが残っており、同期で新旧2人に分裂する恐れ）。
そのため「月初に新姓で追加→締め後に旧姓を削除」の運用とし、新規追加が末尾に行く問題を表示順機能で解決。

### 追加された機能
- 管理者画面のスタッフ一覧に▲▼ボタンを追加。並び順は設定としてクラウド同期され、
  打刻画面の「おなまえ」プルダウンにも同じ順序で反映される。

### 改姓時の運用手順（重要）
1. 給与の締め（月初）まで旧姓のまま打刻を続ける
2. 月初に新姓でスタッフを追加し、▲▼で表示位置を調整
3. 前月分のExcel提出が終わったら旧姓のスタッフを削除（過去データはスプレッドシートに旧姓のまま残る）
4. 有給管理簿（年次有給休暇管理簿）のB列の氏名も新姓に書き換える（有給欄の自動照合が名前一致のため）

## 2026-07-14: スタッフ削除がすぐ復活する不具合を修正（担当: Claude Code）

### 症状
管理者画面でスタッフの「削除」を押しても、数秒後に復活してしまう。

### 原因
打刻データには clientUpdatedAt による新旧判定があるが、**設定（スタッフ名簿）には無かった**。
削除→クラウド保存(no-cors・完了を待てない)→ その保存が反映される前に3秒ポーリング/フォーカス同期が
削除前の古い名簿を取得 → setStaffList で上書き → 削除したスタッフが復活していた。

### 修正
設定にもタイムスタンプ(settingsUpdatedAt)による新旧判定を導入。
- GAS: saveSettings で受信した settingsUpdatedAt を AppSettings に保存、getSettings で返却。
- フロント: saveSettingsToGAS で変更時刻を付与しメモリrefに記録。syncSettingsFromGAS では
  クラウドの settingsUpdatedAt が自端末の変更時刻より古ければ名簿・PINの反映をスキップ（削除を保持）。
  クラウドが追いついたら通常同期に戻る。refはメモリのみ（リロードで自然リセット、保存失敗時の固着を回避）。
  月次確認(confirmations)は別系統のため新旧判定に関わらず常に反映。

### GASデプロイ履歴
- バージョン26 (2026-07-14): 設定のsettingsUpdatedAt対応（このログ記載時点で要デプロイ）

