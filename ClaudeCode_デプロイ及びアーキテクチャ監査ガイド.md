# タイムカードアプリ デプロイ・全体アーキテクチャ設計＆障害対策完全ガイド
（Claude Code 監査・デプロイ指示用マニュアル）

> **作成日時**: 2026-07-02  
> **対象プロジェクト**: スマート出退勤管理システム (`timecard`)  
> **目的**: クロードコード（Claude Code）等のAIツールやエンジニアが、本アプリの全体設計・過去の障害と対策内容を正確に把握し、安全なデプロイおよび今後の不具合根絶に向けた構造監査を実施できるようにすること。

---

## 1. システム全体アーキテクチャ概要

本システムは、店舗に設置したPCおよびスマートフォンから出退勤の打刻・集計を行うWebアプリケーションです。

### 技術スタック
- **フロントエンド (`timecard.html`)**:
  - バニラHTML + CDN経由の React 18 / Babel / TailwindCSS によるシングルファイルアプリケーション（SFA）。
  - ビルドツール不要で、HTML1枚のみで動作。Vercelにて静的配信（`https://timecard-one.vercel.app/`）。
  - ローカルストレージ (`localStorage`) を1次キャッシュとして使用し、オフライン時・起動直後の高速表示を実現。
- **バックエンド (`gas_backup.gs`)**:
  - Google Apps Script（GAS）ウェブアプリエンドポイント（`doGet`, `doPost`）。
  - Googleスプレッドシートをデータベースとして活用。打刻データ（`マスタ` およびスタッフ個別シート）と設定データ（`AppSettings` シート）を管理。

---

## 2. これまでの障害・不具合履歴と対応修正サマリー

これまでに発生した不具合と、その根本原因・実施した修正対応の全履歴です。今後の設計監査時は、これらのバグの再発や類似パターンの見落としがないかを重点的にチェックしてください。

### ① スタッフ選択時の画面真っ白（クラッシュ）問題 (2026/04/21)
- **原因**: スプレッドシート側のシートやレコードを手動削除した結果、「ローカル（端末側）にはデータがあるが、クラウド（GAS）側にはデータがない」という不整合が発生。同期マージ処理で `null` プロパティを参照しようとして React がクラッシュ。
- **対応**: クラウドにデータがない場合の安全マージ分岐を追加。また不正データ（`null` や欠損オブジェクト）を自動除外するデータバリデーション層を追加。

### ② 接続テストボタンの「Invalid action」エラー (2026/04/21)
- **原因**: 管理者画面の接続テストボタンが GAS エンドポイントへアクセスする際、必要なクエリパラメータ `?action=ping` を付与していなかった。
- **対応**: `?action=ping` を正確に付与し、GAS側からの `{ status: 'ok' }` レスポンスを判定して接続成功を表示するよう修正。

### ③ GASコード破損・文字化け問題 (2026/04/21)
- **原因**: エディタや通信のトラブルにより、保存時にコードが途切れ、日本語コメント等が文字化け・構文エラーを起こした。
- **対応**: バックエンドスクリプト (`gas_backup.gs`) を完全な構文と正しい文字コード（UTF-8）で再構築。

### ④ 複数端末間での更新コンフリクト・データ先祖返り問題
- **原因**: 複数PC（店舗PC、オーナー様PC等）で同時にアプリを開いた際、同期タイミングのズレによって古い端末のデータで新しいデータを上書きしてしまう現象が発生。
- **対応**:
  1. **タイムスタンプ優先マージ**: すべてのレコードに端末更新時刻 `clientUpdatedAt` を持たせ、常に最新時刻のデータを勝者とするロジックを実装。
  2. **論理削除 (Tombstone)**: レコード削除時は物理削除せず、`deleted: true` フラグ（スプレッドシート12列目）を立てることで、他端末の古いキャッシュから削除済データが復活するのを完全に防止。
  3. **GAS排他制御**: `LockService.getScriptLock().waitLock(10000)` を実装し、複数リクエストの同時書き込み競合を防止。

### ⑤ GAS URL更新時の各PC追従漏れ（404参照）問題
- **原因**: GASを新規デプロイして新しいURLが発行された際、フロントエンド (`timecard.html`) の初期値を書き換えても、各PCのブラウザには `localStorage('timecard_gas_url')` に古いURLが残り続け、通信エラーを起こしていた。
- **対応**:
  - **自動マイグレーション機能**: `timecard.html` 内の `newUrl` 定数と `localStorage` の値を比較し、不一致時は自動的に新しい `newUrl` を優先して `localStorage` を上書きする処理を搭載。
  - **フォールバック機能**: 通信エラーが `HTTP 404` の場合、自動で有効な予備エンドポイント (`fallbackUrl`) に切り替えて同期を試みる自動復旧処理を追加。

### ⑥ スタッフ名簿設定 (`AppSettings`) の初期値巻き戻り問題 (2026/07/02 本日の事象)
- **原因**: 初回起動時・ローカル開発用の初期値名簿（`["草野", "田中", ...]`）をキャッシュに保持したままのブラウザ・古い端末から管理者画面にアクセスし、設定保存処理 (`saveSettingsToGAS`) が作動。その結果、クラウド本番の `AppSettings` シートが初期値データで上書き保存された。その後、正常な端末が起動した際に同期処理 (`syncSettingsFromGAS`) が作動し、クラウドの初期値をダウンロードして全端末の名簿が初期値へと巻き戻ってしまった。
- **対応**:
  1. **完全リストア**: 過去の打刻レコード（全400件強）から実際に稼働しているスタッフ20名分のフルネーム・時給・外注フラグを抽出し、GAS本番環境へ再プッシュ完了。
  2. **二重バックアップ機能の追加実装 (`gas_backup.gs`)**:
     - `saveSettings` 実行時に、現在の設定を上書きするだけでなく、スプレッドシート内の別タブ **`AppSettingsHistory`** （設定変更履歴シート）へ日時と JSON 文字列を自動追記。
     - 定期実行関数 `recordDailySettingsBackup()` を新設（GASトリガーで毎日自動バックアップ可能に）。

### ⑦ 障害⑥の恒久対策＋通信高速化の実装 (2026/07/02 Claude Code作業・デプロイ済み)

**A. 初期値上書きの完全防止（障害⑥の根本対策）**
- **発見した真の原因**: `timecard.html` 内の「賄い設定一括リセット」マイグレーション（2026/6/21導入）が、新端末・キャッシュクリア後のブラウザで起動直後に自動発火し、初期値名簿12名＋PIN`0000`で本番 `AppSettings` を上書きしていた。「誰かが古い端末を触った」のではなく**新端末で開くだけで再発する状態だった**。
- **フロント側の三重ガード（`saveSettingsToGAS`）**: ①名簿が空・不正型なら拒否 ②初期値名簿（`DEFAULT_SEED_NAMES`）と一致したら拒否 ③クラウドから設定を一度も受信していない端末（`timecard_settings_hydrated`フラグ未設定）は拒否
- **GAS側の最終防衛線（`validateIncomingSettings`）**: 初期値名簿・空名簿・名前欠損を拒否。現名簿10名以上→半数未満への縮小要求も誤送信とみなし拒否
- **上書き前自動退避**: `saveSettings` 実行時、上書き**前**の設定を `AppSettingsHistory` に「[上書き前の自動退避]」として記録（ロールバック可能）
- **受信側バリデーション**: `syncSettingsFromGAS` はクラウドから空配列・不正型が来てもStateに反映しない。PINは4桁数字チェック
- **打刻キューの強化**: キューファースト方式＋確認ベース削除（クラウドに実データ反映を確認してからキュー削除）。no-cors通信でのサイレントなデータ欠落を根絶
- **その他**: `localStorage`破損時の安全パース（`safeParseJSON`）、スタッフ名に`_`を含む場合の削除ID分割修正

**B. 通信高速化（getAllキャッシュ）**
- GASの全件取得（全シート走査で5〜20秒）の結果を **CacheServiceに15秒キャッシュ**。応答135KBが1キー100KB制限を超えるため**90KBずつ分割保存**（`putRecordsCache`/`getRecordsCache`）
- 打刻・削除など書き込み時は `invalidateRecordsCache()` で即座に破棄→古いデータは返らない
- 氏名選択時の同期を silent（裏実行）に変更し、体感待ちゼロに
- **実測: 全件取得 20秒→1.3秒（2回目以降）**

**デプロイ状態**: GASバージョン21（2026/07/02 23:17、URL変更なし）＋Vercel自動デプロイ済み。gitコミット: `06a69d5`（ガード）、`ba9a366`（キャッシュ）、`4593f5d`（分割保存修正）

**保守時の注意**:
- `DEFAULT_SEED_NAMES`（フロント・GAS両方に定義）を変更する場合は**両方同時に**変更すること
- 新端末では初回のクラウド同期完了まで設定保存がブロックされる（数秒）。これは仕様（障害⑥ガード）
- キャッシュ関連の不具合を疑う場合は、GASの `getRecordsCache` が `null` を返せば従来動作に自動フォールバックする設計

---

## 3. クロードコードへのデプロイ指示書（安全・URL変更なし手順）

クロードコードに本番環境への更新・デプロイを行わせる際は、以下の手順に従わせてください。

### バックエンド (Google Apps Script) のデプロイ手順
**★ 重要: 「新しいデプロイ」は作成しないこと！既存のURLを維持するため必ず以下の手順を実行する。**

1. `gas_backup.gs` の内容をコピーする。
2. Google Apps Script エディタ（`https://script.google.com/...`）を開き、コードを全面貼り替えて保存する。
3. 画面右上の **「デプロイ」** → **「デプロイを管理」** をクリック。
4. 左側のリストで現在アクティブな「ウェブアプリ」を選択し、右上の **鉛筆アイコン（編集）** をクリック。
5. 「バージョン」のプルダウンを **「新バージョン」** に変更する。
6. **「デプロイ」** ボタンをクリック。

> **効果**: これにより、発行済みの GAS エンドポイント URL は一切変更されません。店舗PCや各端末での設定変更・URL貼り替えは完全に不要です。

---

## 4. クロードコードへの全体設計監査＆堅牢化指示（プロンプト例）

今後、構造的なエラーや巻き戻り不具合を起こさないために、クロードコードに対して以下の観点でコード監査・リファクタリングを指示してください。

```markdown
以下の観点で `timecard.html` および `gas_backup.gs` のコード全体を詳細に監査し、必要な潜在バグ対策とエラーハンドリング強化を実装してください。

1. **初期値データによる本番クラウド意図せぬ上書きの完全防止**
   - 現在、`timecard.html` で初期値としてハードコードされているスタッフ名簿が、何らかの理由でクラウドに送信された場合でも、本番環境のデータが誤って破壊・上書きされないようにする安全設計（例: ローカルのデータがデフォルト初期値かどうかを判定するフラグや、サーバー側のバリデーションチェック）を検討・実装してください。

2. **オフライン・ネットワーク障害時の書き込み・再送キュー強化**
   - 電波が悪い状態や通信タイムアウト時に打刻または設定変更を行った際、データが欠落したり中途半端な状態にならないよう、通信失敗時の再送ロジックやローカルキュー管理の健全性を検証してください。

3. **双方向同期マージ処理の型安全性と例外ハンドリング**
   - GASから返る JSON データ (`data.settings` や `data.records`) が想定外のフォーマット（空配列、不正型、プロパティ欠損）であった場合にも、React の State や `localStorage` が破壊されず、安全に以前の正常な状態を維持できるよう防御的プログラミングを徹底してください。
```

---

## 5. 現在稼働中のスタッフ一覧（リストア済みマスタ 20名）

万が一手動で名簿を確認・再設定する場合の最新マスタリストです。

1. 草野久美子
2. 小出美穂子
3. 高橋奈々
4. 小田麻美
5. 山田正美
6. 市川悠歩（外注者・時給1226円）
7. 谷口ベアトリス真弥
8. 森川優豪
9. 五十嵐絢子
10. 東洋美
11. 松本阿貴
12. 里田小百合
13. 山本そよか
14. 北島佳歩
15. 田尾理佐
16. いとう
17. 林萌子
18. カワサキ レナン ユキ
19. 藤村祥行
20. 市川（公）
