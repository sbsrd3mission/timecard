# タイムカードアプリ メンテナンスログ

## 2026-04-21: 安定運用版への修正と復旧

### 解決された不具合
1. **スタッフ選択時の画面真っ白（クラッシュ）問題**
   - **原因**: スプレッドシートの過去のシートを削除した際、「スマホ側に記録があるが、スプレッドシート側にデータがない」という状態になった。同期ロジックにおいてこのケースの処理が抜けており、プログラムが `null` データを処理しようとしてクラッシュしていた。
   - **修正**: 「ローカルのみにデータがある場合」の分岐を追加し、安全にデータをマージするように修正。また、不正なデータが保存されていても自動で除去するガードを追加。

2. **接続テストボタンの「Invalid action」表示**
   - **原因**: ボタンがGASのURLにアクセスする際、必要な命令（?action=ping）を付与していなかった。
   - **修正**: `?action=ping` を付けてアクセスし、GASから `ok` の返答があれば成功と表示するように変更。

3. **GASコードの破損と文字化け**
   - **原因**: 前回の作業中にコードが途切れて保存され、日本語コメントが文字化けしていた。
   - **修正**: バックエンドコードを完全に再構築し、最新の日本語環境で復元。

### 現在の技術仕様（安定版: v2.5）
- **同期プロトコル**: 15秒以内の連続更新を防止するロック機構を搭載。
- **データ優先順位**: `clientUpdatedAt`（端末の時間）を最優先。古いバージョンのクラウドデータによる上書きを完全に防止。
- **論理削除（Tombstone）**: データの「完全削除」を行う際、12列目に `true` を書き込む仕様。これにより、他端末で削除したデータが復活するのを防止。

### 万が一の復旧手順
もし動作がおかしくなった場合は、以下のコミット時点のコードに戻してください：
- **Git Commit**: `0596e67` (fix: connection test button uses action=ping)
+
+## 2026-05-02: 利便性向上のための通知機能追加
+
+### 追加された機能
+1. **出勤時の退勤忘れ警告**
+   - **内容**: 出勤ボタンを押した際、過去の最新レコードに退勤打刻がない場合、警告ポップアップを表示。「履歴から修正」または「管理者へ報告」を促す。
+   - **目的**: 月末の集計作業時に発覚する「退勤漏れ」を、翌出勤時に即座に気づけるように改善。
+
+2. **6時間未満勤務の休憩確認**
+   - **内容**: 6時間未満で退勤ボタンを押した際、「休憩は取りましたか？」と確認ポップアップを表示。備考欄への記入を促す。
+   - **目的**: 短時間勤務で休憩が発生した際の記録漏れを防止。
+
+### 安全性への配慮
+- 既存のデータ保存ロジック（GAS連携等）には一切変更を加えておらず、画面表示上の処理追加のみに留めているため、運用への影響はない。
+
